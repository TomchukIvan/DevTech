/*
Console.WriteLine("ПЕРЕДАЧА ВАЛЮТ В ДОЛЛАРЫ");
Console.Write("Введите рублёвую валюту: ");

string input = Console.ReadLine();

if (decimal.TryParse(input, out decimal rubles))
{
    decimal rate = 84.15m; 
    ConvertAndPrint(rubles, rate);
}

void ConvertAndPrint(decimal rub, decimal usdRate)
{
    decimal dollars = rub / usdRate;
    Console.WriteLine($"Сумма в долларах: {dollars:F2} $");
}
*/
